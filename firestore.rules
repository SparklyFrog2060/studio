rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /sensors/{sensorId} {
      allow read, write: if true; // Allow all reads and writes for now
    }
    match /switches/{switchId} {
      allow read, write: if true; // Allow all reads and writes for now
    }
  }
}
